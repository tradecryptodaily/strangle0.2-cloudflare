// ── Your site password ────────────────────────────────────────────────────
// This is the ONLY file you need to touch. Change the value below to
// whatever password you want, save, then:
//   git add -A && git commit -m "set password" && git push
// Cloudflare Pages redeploys automatically (~30s) and the new password takes
// effect. No Cloudflare Access, no dashboard settings, no third-party login —
// just this file.
//
// Set it to an empty string ("") to turn the password gate off entirely.

export const SITE_PASSWORD = "changeme123";
